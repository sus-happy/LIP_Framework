# LIP Framework (alpha)

## How to use.

    $LIP = new LIP_Boot();
    $controler = $LIP->get_control( $mode, $func );

## URL Analayze

    define( "RIP_AUTO_CONTROL", TRUE );
    new LIP_Boot();

# This is Demo System